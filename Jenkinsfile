pipeline {
    agent any 
    stages {
        stage('Build local image from shared Capgemini base image') {
            steps {
	        sh '''
		    echo 'Multiline'
		    echo 'Example'
	        '''
            }
        }
    }
}
